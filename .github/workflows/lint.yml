name: Lint

on:
  workflow_call:
    inputs:
      language:
        description: "Language to lint (bash, python, or all)"
        required: false
        default: "all"
        type: string
      shellcheck-version:
        description: "shellcheck version to use (accepted for backward compat)"
        required: false
        default: "v0.10.0.1"
        type: string
      python-version:
        description: "Python version for py_compile"
        required: false
        default: "3.12"
        type: string
      pre-commit-version:
        description: "pre-commit version to use (accepted for backward compat)"
        required: false
        default: "v4.1.0"
        type: string

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Shellcheck (bash scripts)
        if: inputs.language == 'bash' || inputs.language == 'all'
        run: |
          if ! command -v shellcheck &>/dev/null; then
            echo "shellcheck not preinstalled — installing"
            sudo apt-get update -qq
            sudo apt-get install -y -qq shellcheck
          fi
          # tracked only (no build artifacts); --shell=bash so shebang-less
          # scripts are checked against the caller's declared language.
          # Skip symlinks whose target isn't present on this checkout:
          # shani-pkgbuilds commits run_in_container.sh as a symlink to
          # ../shani-install-media/run_in_container.sh, which shellcheck
          # can't open when the sibling repo isn't checked out alongside
          # (exit 2, "openBinaryFile: does not exist"). `-r` follows the
          # symlink, so it's false for a broken one and true for a real
          # file or a valid symlink — verified live against both shapes.
          files=$(git ls-files '*.sh' '*.bash' | while IFS= read -r f; do
            [[ -r "$f" ]] && printf '%s\n' "$f"
          done)
          if [ -n "$files" ]; then
            shellcheck --shell=bash -S error $files
          else
            echo "No tracked shell scripts — nothing to lint"
          fi

      - name: Python syntax check
        if: inputs.language == 'python' || inputs.language == 'all'
        run: |
          files=$(git ls-files '*.py')
          if [ -n "$files" ]; then
            python3 -m py_compile $files
          else
            echo "No tracked Python files — nothing to check"
          fi